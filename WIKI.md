# WIKI — NanaBanana Studio Bot / image creator INTIMNO

> Полная история создания проекта: от идеи до работающего бота на сервере.  
> Автор проекта: @oreobra  
> Дата: апрель 2026

---

## ОГЛАВЛЕНИЕ

1. [Идея и цель проекта](#1-идея-и-цель-проекта)
2. [Технологии и инструменты](#2-технологии-и-инструменты)
3. [Как строился бот: этапы](#3-как-строился-бот-этапы)
4. [Что было исправлено по ходу](#4-что-было-исправлено-по-ходу)
5. [Итоговый функционал](#5-итоговый-функционал)
6. [Структура файлов](#6-структура-файлов)
7. [GitHub и деплой на сервер](#7-github-и-деплой-на-сервер)
8. [Рабочий цикл обновлений](#8-рабочий-цикл-обновлений)

> Этапы разработки: 1–4 базовый функционал, 5–7 расширения и правки.

---

## 1. ИДЕЯ И ЦЕЛЬ ПРОЕКТА

Telegram-бот для генерации профессиональных product-фотографий женского белья (трусы) для бренда **INTIMNO**.

**Задача бота:** помочь создавать красивые изображения трусов без фотографа — через нейросеть. Пользователь присылает фото своих трусов, бот генерирует готовое изображение в нужном стиле.

**Ключевое условие:** без фото трусов генерация не запускается. Трусы — основа каждого кадра.

**Исходный документ:** файл `Прототип — Telegram-бот NanoBanana.md` — там была первоначальная концепция, которая в процессе работы значительно расширилась.

---

## 2. ТЕХНОЛОГИИ И ИНСТРУМЕНТЫ

| Инструмент | Зачем |
|-----------|-------|
| **Python 3.11** | Язык разработки |
| **aiogram 3.x** | Async Telegram-фреймворк с машиной состояний (FSM) |
| **Replicate API** | Платформа для запуска AI-моделей |
| `anthropic/claude-4-sonnet` на Replicate | Анализ референс-изображений, создание промптов, описание реквизита |
| `google/nano-banana-pro` на Replicate | Генерация изображений белья |
| **aiohttp** | Скачивание готовых PNG с Replicate перед отправкой пользователю |
| **python-dotenv** | Хранение токенов в `.env` файле |
| **Docker + docker-compose** | Запуск бота на сервере в контейнере |
| **GitHub** | Хранение кода, деплой через git pull |
| **VPS-сервер** | Хостинг бота (работает 24/7) |

**Важное решение:** изначально в прототипе был описан запуск через платформу **Antigravity**. В процессе работы от этого отказались — написали полноценный Python-бот. Это дало полный контроль над логикой, гибкость в добавлении функций и независимость от сторонней no-code платформы.

---

## 3. КАК СТРОИЛСЯ БОТ: ЭТАПЫ

### Этап 1 — Первая версия (2 функции)

Первая версия бота включала только то, что было в прототипе:
- **Функция 1 `/reference`** — пользователь присылает референс → Claude анализирует → составляет промпт → пользователь присылает фото трусов → генерация → 1 изображение
- **Функция 2 `/style`** — пользователь присылает фото трусов → выбирает стиль (🌸 Нежный / 🌑 Тёмный) → 5 изображений

---

### Этап 2 — Исправление API-параметров

После получения реальной документации Replicate были исправлены параметры вызовов:

**NanaBanana Pro (до → после):**
```
было:   "width": 768, "height": 1024, "image": BytesIO(...)
стало:  "aspect_ratio": "3:4", "resolution": "1K",
        "output_format": "png", "safety_filter_level": "block_only_high",
        "image_input": [BytesIO(...)]   ← массив, не одиночное поле
```

**Claude Sonnet (до → после):**
```
было:   "prompt": system_prompt + user_prompt (всё в одно поле)
стало:  "system_prompt": ...,   ← отдельное поле
        "prompt": ...,
        "max_tokens": 4096,
        "extended_thinking": False
```

---

### Этап 3 — UX-улучшения (по запросу)

Добавлено по просьбе:

**Меню команд в Telegram:**  
При старте бот регистрирует команды через `bot.set_my_commands()` — они появляются в кнопке меню (иконка `/`) у пользователя.

**Приём изображений файлом:**  
Раньше бот принимал только сжатые фото из галереи. Добавлена поддержка файлов (PNG, JPG, JPEG, WEBP) — через `F.document` с проверкой MIME-типа.

**Отправка результата файлом:**  
Раньше бот отправлял сгенерированные изображения как сжатые фото (`answer_photo`). Это портило качество. Теперь бот скачивает PNG с Replicate через aiohttp и отправляет как документ (`answer_document`) — полное качество без сжатия Telegram.

---

### Этап 4 — Расширение функционала

**Функция 1 — 2 варианта вместо 1:**  
Claude теперь составляет 2 разных промпта по одному референсу. Запускаются 2 генерации параллельно → пользователь получает 2 варианта и выбирает лучший.

**Новая Функция 3 `/describe`:**  
Пользователь описывает желаемый стиль текстом (на русском или английском) → Claude составляет промпт → генерируется 1 изображение. Для тех, у кого нет референсов.

**Меню дополнительных элементов:**  
После получения фото трусов в любой функции бот предлагает добавить реквизит в кадр:

```
[📇 Визитка]         [📖 Журнал INTIMNO]
[✏️ Описать словами] [🖼 Прислать фото]
[✅ Без добавок — генерировать]
```

- **Визитка** → добавляет в промпт описание визитки с брендом INTIMNO
- **Журнал INTIMNO** → добавляет журнал в промпт
- **Описать словами** → пользователь пишет что добавить, текст вставляется в промпт
- **Прислать фото** → Claude анализирует фото реквизита и описывает его для промпта
- **Без добавок** → генерация запускается сразу

---

### Этап 5 — Обновление стилей

**Количество изображений:** уменьшено с 5 до **3** для каждого стиля.

**Добавлены 2 новых стиля:**

| Стиль | Характер |
|-------|---------|
| 💎 Rich | Белый шёлк, мрамор, бархат, золото, жемчуг, журнал INTIMNO, luxury editorial |
| 🎲 Смешанное | Неожиданные сочетания: диван, ноты, ромашки, пушистый плед, свечи |

Итого стилей: **4** (Нежный, Тёмный, Rich, Смешанное), по **3 промпта** каждый.

---

### Этап 8 — Загрузка нескольких фото трусов

**Запрос:** разрешить загружать не одно фото трусов, а несколько — чтобы модель видела больше ракурсов.

**Реализация:** создана отдельная ветка `feature/multi-panties-images` в GitHub, чтобы при необходимости можно было вернуться к старому варианту.

**Что изменилось:**

| Файл | Изменение |
|------|-----------|
| `bot.py` | Старые три хендлера `*_got_panties` заменены одним общим `got_panties_photo`. Каждое присланное фото добавляется в список `panties_file_ids` в FSM-состоянии. После каждого фото бот показывает кнопку «✅ Готово — продолжить». |
| `bot.py` | Новый callback `panties_ready` — при нажатии «Готово» маршрутизирует в правильный следующий шаг в зависимости от текущего flow (reference / style / describe). |
| `bot.py` | Новая клавиатура `PANTIES_READY_KEYBOARD`. |
| `services.py` | `generate_image(prompt, panties_images_bytes: list[bytes])` — принимает список байтов всех фото и передаёт их все в `image_input` NanaBanana Pro. |

**Новый UX загрузки трусов:**
```
Пользователь присылает фото 1 → бот: «✅ Фото добавлено! Пришли ещё или нажми Готово.»
Пользователь присылает фото 2 → бот: «✅ Добавлено фото: 2. Пришли ещё или нажми Готово.»
...
Пользователь нажимает [✅ Готово — продолжить] → бот продолжает флоу
```

---

### Этап 7 — Динамическая генерация промптов для стилей

**Проблема:** в `/style` промпты были фиксированными — 12 текстов, прописанных вручную. Каждый раз при одном и том же стиле генерировались одинаковые кадры.

**Решение:** промпты теперь генерирует Claude на лету перед каждой съёмкой.

**Как это работает:**

1. Вместо фиксированных промптов в `prompts.py` — 4 шаблона-описания стиля (`STYLE_BLUEPRINTS`). Каждый описывает характеристики стиля: поверхности, реквизит, освещение, настроение, углы съёмки.
2. В `services.py` добавлена функция `generate_style_prompts(style_description)` — отправляет шаблон в Claude и получает 3 уникальных промпта.
3. В `bot.py` в момент выбора стиля бот показывает `⏳ Подбираю идеи для съёмки...`, вызывает Claude, затем продолжает обычный флоу с меню реквизита.

**Изменённые файлы:** `prompts.py`, `services.py`, `bot.py`

**Результат:** каждая генерация уникальна — разные поверхности, реквизит, углы — но всегда в рамках выбранного стиля.

---

### Этап 6 — Правки текстов и названий

- Написание исправлено: **NanaBanana** → **Nanobanana Pro** (в /start)
- В промежуточных сообщениях (ожидание, ошибки) — название сервиса убрано, остались нейтральные тексты
- Исправлены тексты `/start` и `/help`: убраны неверные цифры (5 изображений → 3, два стиля → четыре)

---

### Этап 9 — Анализ материала/цвета трусов, убран /style, фидбэк-цикл

**Запрос:** больше разнообразия в генерациях; бот должен понимать материал и цвет трусов и подбирать
сочетающиеся (но не сливающиеся) поверхности/реквизит; убрать выбор из 4 готовых стилей; после каждой
генерации спрашивать у пользователя фидбэк и постепенно учитывать его в будущих генерациях.

**Реализация:** ветка `feature/panties-analysis-feedback-v2`, поверх `feature/multi-panties-images`
(мультизагрузка фото трусов сохранена).

| Файл | Изменение |
|------|-----------|
| `services.py` | Новая `analyze_panties()` — анализирует материал, цвет, принт и характер трусов (по первому загруженному фото), возвращает короткие рекомендации по сочетаемым поверхностям/реквизиту. Используется во всех системных промптах генерации. |
| `bot.py` | Порядок шагов теперь: сначала фото трусов (`panties_ready`) → анализ материала/цвета → затем референс (`/reference`) или описание (`/describe`). |
| `bot.py`, `states.py`, `prompts.py` | Команда `/style` и весь `StyleFlow` удалены (файл `prompts.py` больше не нужен и удалён). |
| `services.py` | Системные промпты переписаны с жёсткого шаблона на более свободный «фото-бриф» с явной инструкцией не повторять типовые заготовки — для разнообразия между генерациями. |
| `bot.py`, `states.py`, `notes.py` (новый) | После генерации бот спрашивает фидбэк (кнопки 👍/👎/пропустить + текст). Текст сжимается моделью в короткую обобщённую заметку и добавляется в `feedback_notes.json` (не более 20 последних), который подмешивается в системные промпты как «learned preferences». |
| `docker-compose.yml`, `Dockerfile` | Добавлен volume `./data:/app/data`, чтобы заметки фидбэка не терялись при пересборке контейнера. |

---

### Этап 10 — Возврат /style (5 стилей), аксессуары в тон, гид-фидбэк

**Запрос:** менеджеры протестировали и попросили вернуть выбор стиля — но не в старом виде
(жёсткие шаблоны без учёта материала/цвета трусов), а поверх уже сделанных улучшений.
Плюс: новый 5-й стиль «по цветотипу», новая опция в допэлементах «аксессуары в тон», и вместо
одного общего вопроса про фидбэк — короткая гид-анкета из нескольких вопросов кнопками.

| Файл | Изменение |
|------|-----------|
| `styles.py` (новый, заменяет старый `prompts.py`) | 5 стилей: 🌸 Нежный, 🌑 Тёмный, 💎 Rich, 🎲 Смешанное — восстановлены из старой версии; добавлен 🎨 Цветотип — генерация, где вся палитра кадра (поверхность, реквизит, свет) осознанно строится вокруг цвета/оттенка трусов (аналоговая/комплементарная/монохромная гармония), а не просто «не сливается». |
| `services.py` | `generate_style_prompts()` — как и в исходной версии, отдаёт **3 варианта** на стиль, но теперь промпт свободный (не жёсткий шаблон) и учитывает анализ материала/цвета трусов + заметки из фидбэка. Также добавлена `suggest_color_matched_accessory()` — короткое предложение аксессуара в тон, на основе анализа трусов (без отдельного фото). |
| `states.py` | Возвращён `StyleFlow` (панти → анализ → выбор стиля → допэлементы). |
| `bot.py` | Команда `/style`: фото трусов → анализ материала/цвета (как в остальных функциях) → выбор из 5 стилей кнопками → 3 промпта → допэлементы. В допэлементы добавлена кнопка «💍 Аксессуары в тон». |
| `bot.py`, `states.py` | Фидбэк переделан в гид-анкету: сначала бот спрашивает согласие («Не против ответить на пару быстрых вопросов?» / Да, давай / В другой раз), затем — если согласие получено — 4 коротких динамических вопроса кнопками + необязательный комментарий текстом. Ответы на конкретные вопросы конвертируются в заметки напрямую (без лишнего обращения к Claude), только свободный комментарий по-прежнему сжимается моделью. |

---

### Этап 11 — Строгая верность гарнмента (июль 2026)

**Проблемы по результатам тестирования:**
- Бот менял трусы в генерации — менял фасон/силуэт, добавлял кружево туда где его нет, менял материал
- Бот описывал цвет и принт трусов в промпте, вместо того чтобы дать модели самостоятельно рассмотреть фото
- При нескольких трусах — размещал их по углам кадра вместо компактной группы

**Решение:** переработан блок `_hard_rules_block()` в `services.py`.

| Правило | Суть |
|---------|------|
| SHAPE / CUT / SILHOUETTE | Фасон (стринг, бикини, трусики...) MUST оставаться как на фото |
| FABRIC / MATERIAL | Материал MUST оставаться идентичным |
| DO NOT ADD WHAT IS NOT THERE | Если на фото нет кружева — никогда не пишем «lace» в промпте |
| Цвет/принт не описываем | В промпте упоминаем только количество + материал. Цвет и принты не описываем — модель видит фото |
| COMPOSITION LAYOUT | Несколько трусов — обязательно компактной группой (веер, каскад, ряд), не по углам |

---

## 4. ЧТО БЫЛО ИСПРАВЛЕНО ПО ХОДУ

### Критические исправления по подсказкам

**1. Неправильные параметры API NanaBanana Pro**  
Изначально использовались `width`/`height` и поле `image`. После того как была получена реальная документация API, исправлено на `aspect_ratio`, `resolution`, `image_input` (массив).

**2. Токены попали в `.env.example`**  
При создании файла `.env.example` в него случайно попали реальные токены. GitHub заблокировал пуш через встроенную защиту (Push Protection / Secret Scanning). Файл был исправлен (заменён на плейсхолдеры), коммит переделан.

**3. Стили: 5 изображений → 3**  
Изначально по прототипу было 5 промптов/изображений на стиль. По запросу изменено на 3 — более разумное количество.

**4. Два стиля → четыре**  
Прототип описывал только Нежный и Тёмный. Добавлены Rich и Смешанное с полными наборами промптов.

**5. Название сервиса в сообщениях**  
Бот писал "Отправляю в NanaBanana Pro" в каждом промежуточном сообщении. По просьбе убрано — оставлено только в `/start`, и написание исправлено на Nanobanana Pro.

---

## 5. ИТОГОВЫЙ ФУНКЦИОНАЛ

### Команды

| Команда | Описание | Результат |
|---------|----------|-----------|
| `/start` | Приветствие и список команд | — |
| `/help` | Подробная справка | — |
| `/reference` | Генерация по референс-изображению | 2 PNG |
| `/style` | Генерация по стилю из каталога | 3 PNG |
| `/describe` | Генерация по текстовому описанию стиля | 1 PNG |
| `/styles` | Описание всех 4 стилей | — |
| `/cancel` | Отменить текущее действие | — |

### Машина состояний

```
ReferenceFlow: waiting_reference → waiting_panties → choosing_extras → waiting_extras_text / waiting_extras_image
StyleFlow:     waiting_panties → choosing_style → choosing_extras → waiting_extras_text / waiting_extras_image
DescribeFlow:  waiting_description → waiting_panties → choosing_extras → waiting_extras_text / waiting_extras_image
```

### Форматы изображений

- **Принимает:** фото из галереи ИЛИ файл (PNG, JPG, JPEG, WEBP)
- **Отдаёт:** PNG-файл в полном качестве (без сжатия Telegram)

---

## 6. СТРУКТУРА ФАЙЛОВ

```
bot-for-images/
├── bot.py            # Все хэндлеры, FSM, тексты сообщений, запуск
├── config.py         # Загрузка переменных из .env
├── states.py         # Состояния FSM (ReferenceFlow, StyleFlow, DescribeFlow)
├── prompts.py        # 4 стиля × 3 промпта = 12 промптов
├── services.py       # Вызовы Replicate: analyze_reference_double,
│                     #   describe_to_prompt, analyze_extras_image, generate_image
├── .env              # Токены (не в Git!)
├── .env.example      # Шаблон переменных
├── requirements.txt  # aiogram, replicate, python-dotenv, aiohttp
├── Dockerfile        # Сборка образа
├── docker-compose.yml # Запуск контейнера с автоперезапуском
├── .dockerignore     # Исключения для Docker
├── .gitignore        # Исключения для Git
├── README.md         # Краткая документация для разработчика
└── Прототип — Telegram-бот NanoBanana.md  # Обновлённый прототип v2.0
```

---

## 7. GITHUB И ДЕПЛОЙ НА СЕРВЕР

### Репозиторий

`github.com/oreobra/image-creator-INTIMNO`

### Ветки

| Ветка | Статус | Описание |
|-------|--------|----------|
| `feature/panties-analysis-feedback-v2` | ✅ **Активная (на сервере)** | Анализ трусов, 5 стилей, Цветотип, фидбэк, строгое сохранение гарнмента |
| `feature/multi-panties-images` | 📦 В архиве | Мультизагрузка фото трусов (вошла в feature-ветку выше) |
| `main` | 📦 Устарел | Старая базовая версия, не отражает актуальный код |

> **Важно:** все изменения ведутся в `feature/panties-analysis-feedback-v2`. Ветка `main` не обновляется.

### Сервер

- **Хост**: `zbbodhuzwo`
- **SSH**: `ssh oreobra@zbbodhuzwo`
- **Директория бота**: `~/bot` (`/home/oreobra/bot`)
- **Запуск**: прямой процесс `python bot.py` от root (без Docker и systemd)
- **Логи**: `/home/oreobra/bot/bot.log`
- **Токены**: хранятся в `~/bot/.env` — нигде больше

### Первый деплой (один раз)

```bash
ssh oreobra@zbbodhuzwo
git clone git@github.com:oreobra/image-creator-INTIMNO.git bot
cd bot
git checkout feature/panties-analysis-feedback-v2
nano .env     # вставить TELEGRAM_TOKEN и REPLICATE_API_TOKEN
pip install -r requirements.txt
sudo bash -c "cd /home/oreobra/bot && nohup python bot.py >> /home/oreobra/bot/bot.log 2>&1 &"
```

### Проверка работы

```bash
ps aux | grep bot.py            # смотреть PID
sudo tail -f /home/oreobra/bot/bot.log   # логи в реальном времени
```

---

## 8. РАБОЧИЙ ЦИКЛ ОБНОВЛЕНИЙ

Всё редактирование — на Mac (в Antigravity), не на сервере.

```
1. Меняем код на Mac
2. git add / git commit / git push origin feature/panties-analysis-feedback-v2
3. SSH на сервер:
   cd ~/bot
   git pull origin feature/panties-analysis-feedback-v2
   ps aux | grep bot.py                    ← смотрим PID
   sudo kill <PID>
   sudo bash -c "cd /home/oreobra/bot && nohup python bot.py >> /home/oreobra/bot/bot.log 2>&1 &"
   ps aux | grep bot.py                    ← проверяем новый PID
```

### Если сервер упал — что делать

Всё необходимое есть на GitHub. Нужно пересоздать только `.env` с токенами (они не хранятся в репозитории!):

```bash
git clone git@github.com:oreobra/image-creator-INTIMNO.git bot
cd bot
git checkout feature/panties-analysis-feedback-v2
nano .env    # TELEGRAM_TOKEN и REPLICATE_API_TOKEN
pip install -r requirements.txt
sudo bash -c "cd /home/oreobra/bot && nohup python bot.py >> /home/oreobra/bot/bot.log 2>&1 &"
```

### Полезные команды

```bash
ps aux | grep bot.py                         # найти PID текущего процесса
sudo tail -f /home/oreobra/bot/bot.log       # логи в реальном времени
git log --oneline -10                        # последние коммиты
git branch --show-current                    # проверить текущую ветку
```

---

*Документ обновлён: июль 2026*
